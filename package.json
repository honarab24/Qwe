{
  "name": "clearkey-mpd-to-hls",
  "version": "1.0.0",
  "description": "Restream ClearKey DASH (.mpd) to HLS (.m3u8) with Shaka Packager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
